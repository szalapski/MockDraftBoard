@page "/"

<h1>Hello, world!</h1>

<div>
    <button @onclick="() => ShowPanel = !ShowPanel">@(ShowPanel ? "Hide" : "Show")</button>
</div>

@if (ShowPanel) {
    <div class="panel1">

        <div>
            Lorem ipsum
        </div>
        <div>
            Lorem ipsum
        </div>
    </div>
}

@code {
    private bool ShowPanel { get; set; } = true;
}